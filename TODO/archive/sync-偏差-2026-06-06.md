# 看板同步偏差归档（2026-06-06 核验发现）

> 本文件归档 2026-06-06 任务管理集中化时发现的看板与代码实际状态不符项。
> 当时已同步到 OPEN.md "看板同步偏差汇总"，现在归档以便 OPEN.md 只保留活跃未完成项。

---

## 偏差汇总表

| 偏差 | 看板说 | 实际 | 同步动作 |
|------|--------|------|----------|
| SAR 分析 Rust 化 | ⬜ | ✅ `core/chem/sar.rs:74,355` 已实现 MCS 提取（用 chematic）+ 单测 | 标 ✅ |
| 分子状态标记 | ⬜ | ✅ `MoleculeReviewPanel.tsx:64` 用 `className={\`molecule-card ${status}\`}` | 标 ✅ |
| B 分子三层迁移 | 🟧 | 比看板更深：schema 迁移（`migrate_v0_to_v1`）已完成 | 改 ⬜ → 🟧（仅 FTS5+sanitize 未做）|
| chematic 编译验证（§六 P0）| ✅ | ✅ cargo check 通过（exit 0） | 保持 ✅ |

---

## 核验方法学

1. **cargo check** 在 `src-tauri/` 目录跑通，确认 chematic 5 个 crate 编译无错
2. **ripgrep** 全文搜索关键字：
   - `trait Table` / `Table\s+trait`（任务 A）
   - `tracing::` / `BudgetEnforcer`（任务 C）
   - `chem_validate` / `chematic_mol` / `schematic_depict` 等
   - `confidence.*threshold`（H1）
   - `low[_-]?confidence`（H2）
3. **文件存在性核验**：`Cargo.toml` chematic 依赖行
4. **context 读取**：每个命中点读前后 3-5 行判断"是真未实现还是名词/占位"

---

## 引发的项目级决策

1. **AGENTS.md §技术债务** 部分描述已与代码现状不符，建议下季度更新
2. **看板状态字段**（⬜/🟧/✅）需建立自动核验机制（CI 中跑 `cargo check` + ripgrep 脚本）
