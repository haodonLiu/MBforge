[package]
name = "mbforge_core"
version = "0.2.0"
edition = "2021"

[lib]
name = "mbforge_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = { version = "0.22", features = ["pyo3"] }
