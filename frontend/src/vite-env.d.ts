/// <reference types="vite/client" />

// Injected by vite.config.ts from frontend/package.json `version` field,
// which is itself kept in sync with constants.yaml `app.version` by
// scripts/generate_constants.py.
declare const __APP_VERSION__: string
